import BodyLogado from './Components/bodyLogado';
import HeaderLogado from './Components/headerLogado';

const PerfilLogado = () => {
  return (
    <main className="bg-gray-200 h-screen">
      <HeaderLogado />
      <BodyLogado />
    </main>
  );
};

export default PerfilLogado;
